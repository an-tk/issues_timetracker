<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "issues_timetracker" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "issues_timetracker" %>">
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield(:head) %>
</head>
<body>
<div id="main" role="main">
  <div class="container">
    <div class="login">

    </div>
    <div class="content">
      <div class="row">
        <div class="span4">
          <ul>
            <li>prject</li>
            <li>project</li>
            <li>project</li>
          </ul>
        </div>
        <div class="span8">
          <%=
              #render 'layouts/messages'
          %>
          <%= yield %>
        </div>
      </div>
      <footer>
      </footer>
    </div>
  </div> <!--! end of .container -->
</div> <!--! end of #main -->
</body>
</html>